setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: backend/Dockerfile
  docker:
    app: frontend/Dockerfile
release:
  image: web
run:
  web: gunicorn cscards.wsgi
  # Heroku should default to using the Dockerfile's run command, which is "npm start"